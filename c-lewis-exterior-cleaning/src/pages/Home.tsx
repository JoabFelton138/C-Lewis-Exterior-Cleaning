import { HomeCarousel } from '../components/HomeCarousel';
import { AboutMe } from '../components/AboutMe';

export const Home = () => {
    return (
        <main>
            <HomeCarousel />
            
            <AboutMe />
        </main>
    );
}; 